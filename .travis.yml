language: c
dist: xenial
arch: ppc64le
cache: ccache
services:
  - docker
#addon:
#  apt:
#    - quota
env:
  global:
    - ARCH=powerpc
    - IMAGE=ubuntu-19.10
      
script:
  - arch
  - df -H
  - quota -A
  - cat /etc/fstab
  
install:
  - docker pull linuxppc/build:$IMAGE-$(uname -m)
