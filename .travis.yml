language: c
dist: xenial
arch: ppc64le
cache: ccache
services:
  - docker

env:
  global:
    - ARCH=powerpc
    - IMAGE=ubuntu-19.10
      
script:
  - arch
  - df -H
  - quota -A
  
install:
  - docker pull linuxppc/build:$IMAGE-$(uname -m)
